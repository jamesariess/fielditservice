# Block direct HTTP access to API files.
# API endpoints must be reached through the router (public/index.php at /api/*),
# which enforces authentication, CSRF, and consistent error handling.
Require all denied